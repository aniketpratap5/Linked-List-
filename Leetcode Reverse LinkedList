class Solution {
    public ListNode reverseList(ListNode head) {
        ListNode nextN= head;
        ListNode curr=head;
        ListNode prev=null;
        while(curr!=null){
            nextN=curr.next;
            curr.next=prev;
            prev=curr;
            curr=nextN;
        }
        return prev;
    }
}
